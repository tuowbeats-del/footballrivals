{
  "name": "footballrivals",
  "version": "1.0.0",
  "private": true,
  "description": "FootballRivals (footballrivals.be) — realtime voetbal draft battles",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd client && npm install --include=dev && npm run build && cd ../server && npm install --omit=dev && cd ../prisma && npm install && npx prisma generate",
    "start": "cd prisma && npx prisma db push --skip-generate && node seed.js && cd .. && node server/index.js"
  }
}
